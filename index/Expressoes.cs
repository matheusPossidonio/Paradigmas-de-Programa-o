using System;
using System.Collections;

namespace index
{
    class Expressoes
    {
        /* static void Main(string[] args)
        {
            //exemplo de uma expressão literal
            //Aqui temos apenas uma expressão que damos um valor a um tipo
            //int numero = 1;

            //exemplo de uma expressao de agregados
            //aqui iremos criar um vetor onde seus 
            //index sao criados a partide de um valor composto
            //int[] vetor = { 1 + 3, 1 + 4, 1 + 5 };

            //exemplo de chamada de expressao
           // int a = 2;


            //exemplo de uma expressao condicional
            // exemplo que podemos dar de facil entendimento é o operador ternario
            var rand = new Random();
            var condition = rand.NextDouble() > 0.5;
            int? x = condition ? 12 : null;

            //exemplo de uma expressao iterativa
            int []numeros = {2,8,5,3,9,4};
            int []numerosPosExpressao = new int[numeros.Length];
            for(int i = 0;i<numeros.Length;i++)
            {
             if(numeros[i]%2==0) numerosPosExpressao[i]=0;
             else numerosPosExpressao[i]=numeros[i];
            }

            for(int j = 0;j<numeros.Length;j++)
            {
             Console.WriteLine(numeros[j] + " ");
            }
            

            //exemplo de acesso a cosntantes e variaveis
            const int DOBRO = 2;
            int numero = 6;
            int numerDobrado = numero *DOBRO;

        }
 */
    }
}
